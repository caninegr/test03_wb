# netlify.toml (in site folder)
[build]
  base = "site"  # 👈 ADD THIS - tells Netlify to run from site folder
  command = "gatsby build"
  functions = "netlify/functions"
  publish = "public"

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200